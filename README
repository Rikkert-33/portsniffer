First enter startport, then endport, then target, to configure your search ratio and target. 

For default start, endport and target, press enter when this is asked.
Standard target is set to "localhost".